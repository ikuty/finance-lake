# edinet-dl

EDINET（金融庁の電子開示システム）から書類ファイルを取得し、生データのまま保存する
レイク層。個人運用のEDINET分析基盤の一部であり、メタデータの列展開・検索用インデックス
作成、および中身の解釈（パース）は後段の別リポジトリが担う。Mac Mini 2012にUbuntu 24.04
LTSを導入したマシン上でDockerを使って動かす。OS用に250GBのSSD(`/`)、ストレージ用に500GB
のHDD(`/home`)を搭載しており、アプリ本体（リポジトリ・`.env`等）・データ（進捗管理DB・
書類本体ファイル）とも`/home`配下（HDD側）に統一して配置する。

## 経緯・背景

- 元々は `ikuty-skills` リポジトリ内の `stock-fundamental-analysis` スキルのデータ取得部分として着手した。
- 当初 IR Bank (irbank.net) をデータソース候補としたが、**利用規約でAI・機械学習目的での利用が明示的に禁止**されており（取得方法を問わず）、採用を断念した。
- 代替として **EDINET API** を採用。金融庁公式・無償・規約上AI利用の制限なし。ただし以下の制約がある:
  - Webサイトの直接スクレイピングは禁止。**書類取得APIの利用はこの禁止の適用除外として明示的に許可**されているため、必ずAPI経由でアクセスすること
  - 短時間の大量アクセスは禁止（運用上の礼儀としてリクエスト間隔を空ける。現状1.2秒/リクエスト）
  - **出典表示が必須**。例:「出典：EDINET閲覧（提出）サイト（該当ページのURL）、PDL1.0」。加工した場合は加工した旨と作成者名も明記
  - AI・機械学習目的の利用制限は無い（IR Bankとの決定的な違い）
- スキルという単位から独立させ、本リポジトリとして切り出した（2026-08-13）。将来的にスキル側からはここで蓄積したデータを参照する形になる想定。

## アーキテクチャ方針（確定事項）

- **サービス分割**: 本リポジトリ（`edinet-dl`）は、EDINETから書類ファイルを取得し生データのまま保存する**レイク層**専任とする。メタデータの列展開・検索用インデックス作成、および中身の解釈（パース）は**後段の別リポジトリ**が担う。取得は外部APIの可用性・レート制限に依存し安定させたい一方、インデックス設計・解釈ロジックは今後何度も変わる見込みであり、変更のたびにEDINETへ再アクセスせず保存済みの生データに対してのみ再処理できることが重要なため。
  - リポジトリを分けることで、後段が本リポジトリの内部実装に依存するのを境界として防ぐ。HTTP APIやメッセージキューは導入せず（over engineering回避）、SQLite/ローカルファイルストレージという共有ストレージを境界にした疎結合な別リポジトリ・別コンテナ・別スケジュールとする。
- **DBの役割**: SQLite(`fetch_progress`テーブル)は日付ごとの取得状況（済み/エラー、件数）の記録専用。書類メタデータの列展開・検索用インデックスは持たない（後段リポジトリの責務）。`/home`配下（500GB HDD）のbind mountに置く。クラウドストレージ（S3等）は使わない。
- **書類本体（実データ）の保存**（実装済み）: 対象は全上場企業（`secCode`が設定されている書類）。`/home`配下のHDDへ保存する。500GBの容量に対して見積もり上十分な余裕があるため、クラウドへのアップロードや取得後の削除は行わない。バックアップ（HDD故障時の復旧手段）は別途検討する。
  - 保存先パス: `data/raw/{fileDate}/{edinetCode}/{type}/{docID}.pdf`（PDF）、`data/raw/{fileDate}/{edinetCode}/{type}/{docID}/（元のzip内パス）.gz`（XBRL/CSV、展開して個別gzip圧縮）。日付を最上位階層にするのは、後段の日次ingestが対象日のディレクトリだけを見れば済むようにするため。`edinetCode`を次の階層にするのは、人間がレイクを直接見たときに企業単位で判別しやすくするため。type（xbrl/csv/pdf）をさらに次の階層にするのは、後段でtype単位の一括読み込みをしやすくするため。docIDを最下層に置くのは、同一企業・同一日に複数docIDがある場合、展開後のファイル名（EDINET側の命名は書類種別ごとに同名になりやすい）が衝突するのを防ぐため。詳細は`docs/file_download_design.md`参照。
  - 大量保有報告書等、提出者の`edinetCode`と報告対象企業の`subjectEdinetCode`が異なる書類では、提出者側のディレクトリに格納される（現時点では許容）。
- **アプリ本体の配置**: リポジトリ・`.env`等のアプリ本体もデータと同じ`/home`配下（HDD側）に統一する。SSD側（`/`）にはOS以外は置かない。
- **実行方式**: Mac Miniホストのsystemdタイマー（`edinet-dl.timer`）が `docker run --rm` を叩くだけ。コンテナ内にcron/systemdデーモンは置かない。`setpci`によるAC電源復帰時の自動起動もsystemctlで管理しており、実行方式をsystemdに統一している。
- **電源管理**: 本体はスマートプラグ（Tapo P110M）経由で毎日定時に電源投入・遮断される。Mac Mini側は`setpci`で「電源投入時にOS自動起動」するよう設定済み。日次ジョブは電源ON〜電源OFFの間に処理を完了してOSをシャットダウンする必要がある（OSシャットダウンが電源OFFより先に完了する前提）。systemdタイマーは`OnCalendar`（固定時刻）＋`Persistent=true`とし、電源投入〜シャットダウンの時間枠内に収まるよう時刻を設計する。`OnBootSec`（起動からの相対時刻）は採用しない。開発時にTailscale経由で手動起動したセッションでもジョブ＋シャットダウンが毎回走ってしまうのを避けるため。systemdのシステムunitはroot権限で実行されるため、shutdown実行にsudoers設定は不要。
- **ネットワーク**: Ubuntuは外部から遮断されたLANに配置し、Tailscaleを導入する。開発時はTailscale+SSHでリモート接続。GitHub ActionsもTailscale+SSHで本機に接続し、モジュール等をデプロイする（CI/CDワークフローの実装は別途）。
- **日次ジョブ**: 当面は書類一覧の取得状況記録のみ。`--days`は指定せず、環境変数`DAYS_WINDOW`
  （既定3日）による遡り窓で実行する。電源障害等でジョブが実行されなかった日や、一時的な
  失敗で`error`になった日も、窓の範囲内なら自動的に再試行される。書類本体のファイル取得は
  将来追加。
- **初回バックフィル**: 過去10年分を手動実行（systemdタイマーには含めない）。`fetch_progress`テーブルにより中断・再開可能。

## 実装言語の選定理由

Python 3.12（stdlib中心）を採用。判断基準は「型付き・テストしやすさ・省メモリ高速・依存管理の容易さ」の4点だったが、本ジョブはEDINET APIへのI/Oバウンドな処理で、かつ意図的にリクエスト間隔を空けているため、省メモリ・高速実行の軸はほぼ効かないと判断。Go等への書き換えは、動作確認済みのコードを捨てて存在しない性能問題を解決することになり、over engineeringと判断して見送った。
- 型付け: 型ヒント + `mypy --strict`
- テスト: `pytest`
- 依存管理: フェーズ1・フェーズ2とも標準ライブラリのみ（依存ゼロ）で完結する見込み。クラウドSDK（`boto3`等）は不要になった。Poetry/uv等の追加ツールもこの依存数では不要と判断。

## 現状（2026-08-13時点）

- フェーズ1（取得・保存サービス）のスキャフォールド完了。`pytest`（7件）・`mypy --strict`ともにパス。
- Dockerビルド確認済み。コンテナ経由で実際にEDINET APIを叩き、2026-08-13分335件のインデックス取得に成功。再実行時のスキップ（冪等性）も確認済み。
- git init実施済み。実機へのデプロイ・crontab登録は未実施。
- 実行環境をVPSからRaspberry Pi 4 Model B(4GB RAM) + USBメモリ(512GB)に変更（2026-08-16）。これに伴いS3フェーズ2は取りやめ、書類本体もローカル保存する方針に変更。
- 実行環境をさらにRaspberry PiからMac Mini 2012(Ubuntu 24.04 LTS) + SSD(OS用250GB)/HDD(`/home`用500GB)構成に変更（2026-08-23）。電源はスマートプラグ（Tapo P110M）による日次スケジュールON/OFFとMac Mini側の`setpci`自動起動設定を組み合わせる。ネットワークはTailscale経由のリモートアクセス（開発・GitHub Actionsデプロイとも）に統一。

## 次にやること（未着手）

- 初回10年分バックフィルの実行（本物のAPIキーを`.env`に設定した上で手動実行）
- リモートリポジトリ整備
- Mac Miniへのデプロイ、systemdタイマー登録（`systemd/edinet-dl.service`・`systemd/edinet-dl.timer`参照、電源投入〜シャットダウンの時間枠内に収まるようスケジュール調整）
- GitHub Actions（Tailscale+SSH経由）によるデプロイワークフローの実装
- フェーズ2: ウォッチリスト機能・書類本体取得・`/home`配下HDDへの保存
- 解釈（パース）サービスの設計（別サービスとして、将来着手）

## 参照

- EDINET API仕様: `type=5`指定でXBRLをCSV化した形で取得可能（UTF-16LE、タブ区切り）。生XBRL/XMLパースより大幅に簡単。
- 有報CSVのデータカタログ（項目一覧・注意点）は `ikuty-skills` リポジトリの
  `skills/stock-fundamental-analysis/references/edinet_yuho_csv_catalog.md` に詳細あり。
  特に「連結/個別の混在」はコンテキストIDの完全一致判定が必須（`_NonConsolidatedMember`サフィックスの有無で区別、`startswith`等の部分一致は危険）という実務上の注意点は要参照。
